This is yaml-cpp without modifications, built statically.

Note that it is only (currently) being used in the sub-build of OpenImageIO and OpenColorIO,
which are, in O3DE, tools-only libraries, so we don't have a build of this for non-tool targets such
as android.
